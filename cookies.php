<?php
	require_once 'register-controller.php';
// por si se quiere tener una cookie temporal

	// $expirationTime = time() + 1800;
	// setcookie('temporal', 'durará por 30mins', $expirationTime);
	
	// setcookie('temporal', '', time() - 10);

	
?>